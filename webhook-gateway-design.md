# 웹훅 수신 게이트웨이 — 설계 문서

**상태**: 설계 단계
**기준 버전**: MySQL 8.0, Spring Boot 3.x, Java 21
**최종 수정**: 2026-09

---

## 1. 문제 정의

### 1.1 현재 방식

외부에서 웹훅을 받는 가장 흔한 코드는 컨트롤러 하나다.

```java
@PostMapping("/webhooks/payment")
public ResponseEntity<Void> handle(@RequestBody PaymentEvent event) {
    verifySignature(...);
    orderService.markPaid(event.getOrderId());
    inventoryService.decrease(...);
    mailService.send(...);
    return ResponseEntity.ok().build();
}
```

평상시에는 800ms에 끝나고 문제가 없다. 아래 여섯 가지 상황에서 깨진다.

### 1.2 실패 모드

#### FM-1. 처리 시간이 곧 수신 응답 시간이 된다

SMTP가 느려져 `mailService.send()`가 3.5초를 먹으면 전체 처리가 4초가 된다. PG사 타임아웃이 3초라면 실패로 판정하고 재시도 큐에 넣는다. 우리는 4초에 처리를 끝내고 200을 보냈지만 이미 늦었다.

30초 뒤 같은 웹훅이 다시 온다. 컨트롤러는 그것이 재시도인지 모른다. `inventoryService.decrease()`가 다시 실행되어 재고가 2개 빠진다.

**원인**: 수신과 처리가 한 스레드에 묶여 있다.

#### FM-2. 배포 중 유실되고, 제공자가 재시도해준다는 보장이 없다

롤링 배포 중 30초 구간에 요청이 떨어진다. PG사는 재시도해주므로 괜찮다. 그러나 같은 서버가 GitHub 웹훅도 받고 있다면 다르다. GitHub은 실패한 웹훅을 자동 재시도하지 않고 대시보드에서 수동 재전송만 지원한다. 그 30초 사이의 push 이벤트는 아무도 모르게 사라진다.

**원인**: 재시도 정책은 제공자마다 다르고, 안 해주는 곳이 있다. 해주는 쪽만 보고 설계하면 안 해주는 쪽에서 데이터를 잃는다.

#### FM-3. 정상 처리했는데도 중복이 온다

800ms에 처리를 끝내고 200을 보냈지만 응답 패킷이 유실됐다. 제공자 입장에서는 응답이 없는 것이므로 재시도한다.

**원인**: 웹훅은 사실상 전부 at-least-once다. exactly-once는 분산 트랜잭션 없이 불가능하므로 구조적으로 제거할 수 없다.

#### FM-4. 나중 이벤트가 먼저 도착한다

구독 플랜 변경 시나리오.

| 시각 | 발생 | 도착 |
| --- | --- | --- |
| 14:00:01 | `plan = PRO` | 두 번째로 도착 |
| 14:00:02 | `plan = ENTERPRISE` | 첫 번째로 도착 |

상태를 덮어쓰는 로직이면 최종값이 PRO가 된다. 사용자는 ENTERPRISE 요금을 냈는데 PRO를 쓴다.

#### FM-5. 실패한 요청의 흔적이 없다

3일 전 요청이 500으로 떨어졌다면 페이로드가 어디에도 없다. 컨트롤러가 예외를 던지고 끝났기 때문이다. 제공자 대시보드에 남아 있으면 다행이고, 보관 기간이 지났으면 복구 불가다.

#### FM-6. 소비 로직 버그를 뒤늦게 발견해도 되돌릴 수 없다

GitHub push 웹훅에서 브랜치명을 뽑는 로직에 버그가 있어 `refs/heads/feature/login` 같은 슬래시 포함 브랜치가 2주간 잘못 저장됐다. 버그는 고쳤지만 다시 흘릴 원본이 없다.

### 1.3 핵심 명제

> 웹훅 수신은 HTTP 요청 처리 문제가 아니라 **메시지 수집 문제**다.

컨트롤러에 비즈니스 로직을 붙이는 순간 축을 잘못 잡은 것이고, FM-1부터 FM-6까지가 그 결과다. **받아서 저장하고 즉시 응답하고, 처리는 별도 워커가 한다.** 수신과 처리를 시간축에서 분리하는 것이다.

### 1.4 여기서 별도 서버가 나오지는 않는다

**중요한 구분이다.** 위 여섯 개는 전부 **아웃박스 패턴**으로 해결된다. 애플리케이션이 직접 웹훅을 받되, 원본을 자기 DB에 저장하고 즉시 200을 준 뒤, 워커가 나중에 처리하면 된다.

| 실패 모드 | 애플리케이션 내부 아웃박스로 해결되나 |
| --- | --- |
| FM-1 타임아웃 → 중복 | 된다 |
| FM-2 배포 중 유실 | 된다 (수신부가 얇아 배포 영향 구간이 짧다) |
| FM-3 응답 유실 → 중복 | 된다 (멱등키) |
| FM-4 순서 역전 | 된다 (소비자가 버전 판정) |
| FM-5 흔적 없음 | 된다 (원본을 아웃박스에 보관) |
| FM-6 재생 불가 | 된다 (원본이 남아 있으므로) |

**여섯 개 모두 별도 서버 없이 해결된다.** 제공자 하나, 받는 서비스 하나라면 150줄 정도로 끝나고, 그게 정답이다.

그러므로 이 문서의 논증은 다음과 같아야 한다.

```
여섯 가지 실패 모드
  → 수신과 처리를 분리해야 한다        (아웃박스로 충분)
  → 서비스가 여럿이면 그 분리 로직이 N벌 생긴다
  → 그때부터 한 곳으로 모을 가치가 생긴다
```

### 1.5 언제 모을 가치가 생기나

게이트웨이의 가치는 **문제 해결이 아니라 중복 제거**다. 아래 조건 중 **둘 이상**이면 검토할 만하다.

| 조건 | 내용 |
| --- | --- |
| 제공자가 여럿 | 서명 형식, 이벤트 ID 위치, 재시도 정책이 제공자마다 다르다. 서비스마다 각자 구현하면 각자 다르게 틀린다 |
| 받는 서비스가 여럿 | 결제·배송·상품이 각각 웹훅을 받으면 원장과 재시도가 세 벌 생긴다 |
| 원본 보존이 감사 요건 | 아웃박스는 처리 후 정리하는 것이 보통이라 이 요건과 맞지 않는다 |
| 인터넷 노출을 줄여야 함 | 직접 받으면 각 서비스가 인터넷에 열려야 한다. 모으면 한 곳만 열면 된다 |

**조건이 하나 이하면 애플리케이션이 직접 받는 편이 낫다.** 서버를 하나 늘리는 비용이 더 크다.

### 1.6 제공자별 차이 — "모은다"의 실제 내용

|  | GitHub | Stripe | Slack |
| --- | --- | --- | --- |
| 서명 헤더 | `X-Hub-Signature-256` | `Stripe-Signature` | `X-Slack-Signature` |
| 헤더 형식 | `sha256=<서명>` | `t=<시각>,v1=<서명>` | `v0=<서명>` |
| 서명 대상 문자열 | 바디만 | `시각 + "." + 바디` | `버전:시각:바디` |
| 이벤트 ID 위치 | 헤더 `X-GitHub-Delivery` | 바디 `event.id` | 헤더 |
| 타임아웃 | 10초 | 20초 | 3초 |
| 재시도 | **하지 않음** | 3일간 | 3회 |

**이 표가 곧 코드다.** 서비스마다 각자 구현하면 특히 서명 대상 문자열 조합에서 틀리기 쉽고, 그건 테스트로 잘 잡히지 않는다.

---

## 2. 이것은 무엇인가

### 2.1 한 문장 정의

> **외부 서비스가 보내는 웹훅을 애플리케이션 대신 받아, 원본 그대로 저장한 뒤, 애플리케이션이 처리에 성공할 때까지 책임지고 전달하는 별도의 중계 서버.**

**별도로 배포되는 서버다.** 라이브러리도, 애플리케이션 안의 모듈도 아니다. 독립된 프로세스로 뜨고 자기 DB를 갖는다.

### 2.2 배치 위치

**도입 전**

```
토스페이먼츠 ──POST──▶ 주문 서비스
                       POST /webhooks/payment
                       (서명 검증 + 주문 변경 + 재고 + 메일을 한 스레드에서)
```

**도입 후**

```
토스페이먼츠 ──POST──▶ 게이트웨이 ──POST──▶ 주문 서비스
                      (별도 서버)          POST /internal/payment-events
                      + 자체 DB            (주문 변경 + 재고 + 메일)
```

**주문 서비스가 하는 일은 그대로다.** 달라지는 건 요청이 들어오는 출처뿐이다. 토스페이먼츠 대신 게이트웨이가 부른다.

바뀌는 것은 셋이다.

| | 도입 전 | 도입 후 |
| --- | --- | --- |
| 응답 제한 시간 | PG사 타임아웃 3초 | 없음. 게이트웨이가 기다려준다 |
| 실패 시 | 제공자 재시도 정책에 의존 | 게이트웨이가 재시도 |
| 페이로드 보관 | 없음 | 게이트웨이 DB에 원본 보존 |

### 2.3 경계 — 하지 않는 일

게이트웨이는 **페이로드를 해석하지 않는다.** JSON 파싱조차 하지 않는다. `{"orderId": 123}`이 들어와도 게이트웨이에게는 그냥 바이트 27개다.

주문 상태 변경, 재고 차감, 메일 발송은 **여전히 주문 서비스가 한다.** 게이트웨이는 그 일을 대신하지 않는다.

등기우편 재배달과 같은 성격이다. 봉투를 열지 않고, 받는 사람이 없으면 보관했다가 다시 배달하고, 배달 이력을 남긴다. 내용물이 뭔지는 관심이 없다.

### 2.4 용어

이 문서 전체에서 쓰는 말들이다.

| 용어 | 정의 |
| --- | --- |
| **제공자 (provider)** | 웹훅을 보내는 외부 서비스. 토스페이먼츠, GitHub, Stripe |
| **소비자 (consumer)** | 웹훅을 실제로 처리하는 우리 애플리케이션. 위 예시의 주문 서비스 |
| **수신부** | 게이트웨이에서 제공자의 HTTP 요청을 받는 부분. 컨트롤러 하나 |
| **원장 (ledger)** | 받은 것을 원본 그대로 쌓아두는 DB 테이블 |
| **전달부** | 원장에 쌓인 것을 소비자에게 보내는 워커 |

**원본 바이트란**

HTTP 바디로 들어온 바이트 열 그대로를 말한다. 파싱하지도, 객체로 바꾸지도, 다시 직렬화하지도 않은 상태.

```
제공자가 보낸 것:  {"orderId":123, "status":"DONE"}
                   └─ 이 32바이트를 그대로 저장한다

DTO로 받아 재직렬화하면: {"orderId":123,"status":"DONE"}
                        └─ 공백이 사라졌다. 다른 바이트다
```

눈으로는 같아 보여도 바이트가 다르고, 그러면 서명 검증이 깨진다. 이유는 아래.

**서명 검증이란**

웹훅 엔드포인트는 인터넷에 열려 있다. 주소만 알면 누구나 POST할 수 있다. 아무나 "결제가 완료됐다"는 가짜 요청을 보내면 주문이 결제완료로 바뀐다.

그래서 제공자와 우리만 아는 비밀키(시크릿)를 미리 공유하고, 제공자는 요청을 보낼 때 그 키로 바디의 해시값을 계산해 헤더에 담는다.

```
제공자 쪽:  서명 = HMAC-SHA256(시크릿, 바디)
            헤더에 담아서 전송

우리 쪽:    같은 시크릿으로 받은 바디의 해시를 다시 계산
            헤더의 값과 일치하면 진짜, 아니면 401
```

**해시는 바디 바이트에 대해 계산된다.** 그래서 바이트가 한 개만 달라도 값이 완전히 달라지고, 재직렬화한 바디로는 검증이 통과할 수가 없다. 2.4의 "원본 바이트"가 중요한 이유가 이것이다.

**아웃박스(outbox)란**

여러 번 나오는 용어이므로 여기서 정의한다. **지금 당장 할 수 없는 일을, 같은 트랜잭션 안에서 DB 테이블에 적어두고 나중에 워커가 처리하는 패턴**이다.

전형적인 문제는 이렇다.

```java
@Transactional
public void handlePayment(String orderId, PaymentEvent event) {
    orderRepository.markPaid(orderId);                 // ① MySQL
    kafkaTemplate.send("payment.completed", event);    // ② Kafka
}
```

`@Transactional`은 MySQL만 관리한다. Kafka는 롤백 대상이 아니고, `send()`는 기본이 비동기라 커밋 이후에 실패할 수도 있다. 두 방향으로 깨진다.

- ①은 커밋됐는데 ②가 실패 → 주문은 결제완료인데 배송은 영원히 모른다
- ②는 나갔는데 ①이 실패 → 배송은 준비하는데 결제 기록이 없다

**MySQL과 Kafka에 걸치는 트랜잭션은 존재하지 않는다.** 이를 이중 쓰기(dual write) 문제라 한다.

해법은 Kafka에 직접 쏘지 않고 같은 DB의 테이블에 넣는 것이다.

```java
@Transactional
public void handlePayment(String orderId, PaymentEvent event) {
    orderRepository.markPaid(orderId);     // 같은 MySQL
    outboxRepository.insert(event);        // 같은 MySQL, 같은 트랜잭션
}
```

이제 둘은 원자적이다. 발행은 별도 워커가 `published_at IS NULL`인 행을 읽어서 한다. 이름은 발신함에서 왔다. 우체통에 넣어두면 집배원이 나중에 가져가는 구조다.

**게이트웨이의 원장이 이것과 같은 구조다.** 저장 대상이 "발행할 이벤트"가 아니라 "받은 웹훅 원본"이고, 워커가 보내는 곳이 Kafka가 아니라 소비자의 HTTP 엔드포인트인 것만 다르다.

---

## 3. 배포 형태 — 별도 서버는 선택이다

수신부 로직을 어디에 둘지는 세 가지 선택지가 있다. **기능은 동일하고 배포 단위만 다르다.**

|  | A. 각 서비스가 직접 | B. 별도 서버 | C. 라이브러리(스타터) |
| --- | --- | --- | --- |
| 서버 수 | +0 | +1 | +0 |
| 코드 중복 | N벌 | 1벌 | 1벌 |
| 원본 저장 위치 | 각 서비스 DB | 게이트웨이 DB | 각 서비스 DB |
| 비즈니스 트랜잭션과 묶기 | 가능 | **불가** | 가능 |
| 인터넷 노출 | 각 서비스 | **게이트웨이만** | 각 서비스 |
| 수신 로직 수정 시 | 각 서비스 수정 | 게이트웨이만 배포 | **전 서비스 재배포** |
| 언어 제약 | 없음 | 없음 | **JVM만** |

**B가 이기는 조건은 두 가지뿐이다.** 인터넷 노출을 한 곳으로 몰아야 하거나, 서비스 언어가 섞여 있거나. 둘 다 아니면 C가 A와 B의 장점을 대부분 가져간다.

**이 문서는 B를 기준으로 서술한다.** 다만 그것이 유일한 답이 아니며, 위 조건에 따라 A나 C가 맞을 수 있다는 점을 명시한다.

| 원칙 | 내용 |
| --- | --- |
| P1 | 수신 경로에서는 검증과 저장만 한다. 비즈니스 로직 없음 |
| P2 | 원본 바이트를 손상 없이 보존한다 |
| P3 | at-least-once를 전제한다. exactly-once를 목표로 삼지 않는다 |
| P4 | 모든 상태 변화는 append-only 이력으로 남긴다 |
| P5 | 순서 보장은 기본값이 아니다. 필요한 소비자가 스스로 판정한다 |
| P6 | 페이로드를 해석하지 않는다. 내용에 대한 판단은 소비자 몫이다 |

---

## 4. 설계 원칙

| 원칙 | 내용 |
| --- | --- |
| P1 | 수신 경로에서는 검증과 저장만 한다. 비즈니스 로직 없음 |
| P2 | 원본 바이트를 손상 없이 보존한다 |
| P3 | at-least-once를 전제한다. exactly-once를 목표로 삼지 않는다 |
| P4 | 모든 상태 변화는 append-only 이력으로 남긴다 |
| P5 | 순서 보장은 기본값이 아니다. 필요한 소비자가 스스로 판정한다 |
| P6 | 페이로드를 해석하지 않는다. 내용에 대한 판단은 소비자 몫이다 |

**P6이 경계선이다.** 페이로드를 해석하지 않는 일은 모든 웹훅에 공통이므로 모을 수 있고, 해석하는 순간부터 제공자별·서비스별로 갈라진다. 게이트웨이가 페이로드를 열어보기 시작하면 "공통 부분만"이라는 성질이 깨진다.

---

## 5. 아키텍처

```
외부 제공자
    │
    ▼
┌─────────────────────────────────────────────────┐
│  게이트웨이                                      │
│  ┌──────────┐   ┌──────────┐   ┌──────────┐    │
│  │  수신부   │──▶│   원장    │──▶│  전달부   │    │
│  │ 서명 검증 │   │ 원본 저장 │   │ 재시도    │    │
│  │ 즉시 200  │   │ 멱등 차단 │   │ 데드레터  │    │
│  └──────────┘   └──────────┘   └──────────┘    │
└─────────────────────────────────────────────────┘
    │
    ▼
  소비자
```

**관리 평면**은 위 흐름 바깥에 있다. 엔드포인트 등록, 시크릿 관리, 목적지 설정, 조회, 재생 실행을 담당한다.

### 5.1 무엇을 만들고 무엇을 안 만드나

| 구성 | 정체 | 만드나 |
| --- | --- | --- |
| 제공자 | 토스페이먼츠, GitHub 등 외부 서비스 | 아니다. 이미 있다 |
| **게이트웨이** | **Spring Boot 애플리케이션** | **이게 결과물이다** |
| **게이트웨이 DB** | **MySQL. 원장 + 큐** | **스키마를 만든다** |
| 소비자 | 우리 애플리케이션 (주문 서비스 등) | 아니다. 이미 있다 |

즉 **박스 하나를 만든다.** 4개 구성요소(수신부, 원장, 전달부, 관리 평면)는 그 안의 논리적 구획이지 별도 서버가 아니다. 하나의 Spring Boot 프로세스 안에 컨트롤러, DB 테이블, `@Scheduled` 워커로 들어간다.

**검증할 때는** 제공자와 소비자를 흉내 낼 것이 필요하다. 발신 스크립트와 목 소비자를 따로 만들지만, 그건 도구지 결과물이 아니다.

### 5.2 요청 하나가 어떻게 흐르는가

토스페이먼츠 결제 완료 웹훅 한 건의 전체 경로다. 주문 서비스 처리가 4초 걸리는 상황을 가정한다.

**정상 경로**

```
14:00:00.000  제공자 → 게이트웨이
              POST /w/toss-payment
              Tosspayments-Signature: t=1725336000,v1=5257a869e7...
              {"eventType":"PAYMENT_STATUS_CHANGED","orderId":"ord_9931","status":"DONE"}

14:00:00.008  서명 검증
              받은 바디 바이트 + 저장된 시크릿으로 HMAC 재계산 → 헤더 값과 일치

14:00:00.021  원장에 INSERT
              id=8842, slug='toss-payment', status='PENDING'
              idempotency_key='evt_2b8f...'  (제공자가 준 이벤트 ID)
              raw_body=<위 JSON의 원본 바이트 그대로>

14:00:00.024  게이트웨이 → 제공자
              200 OK
              ★ 여기서 제공자와의 관계는 끝난다. 총 24ms
              ★ 3초 제한이 사라진 게 아니라, 그 안에서 하는 일을 줄인 것이다
              ★ 아래 처리는 게이트웨이가 정한 타임아웃을 따른다

── 이하는 별도 워커 스레드 ──

14:00:00.900  워커가 큐에서 집음
              SELECT ... WHERE status='PENDING' ... FOR UPDATE SKIP LOCKED
              status='DELIVERING'으로 변경

14:00:00.902  게이트웨이 → 소비자
              POST /internal/payment-events
              X-Gateway-Event-Id: 8842
              {"eventType":"PAYMENT_STATUS_CHANGED",...}   ← 원본 그대로 전달

14:00:04.910  소비자가 주문 변경 + 재고 차감 + 메일 발송 후 200
              ★ 4초 걸렸지만 아무 문제 없다

14:00:04.912  원장 갱신
              id=8842, status='DELIVERED'
```

**핵심은 `14:00:00.024`와 `14:00:04.910` 사이의 단절이다.** 제공자는 24ms에 응답을 받고 떠났고, 소비자는 4초를 썼다. 도입 전 구조에서는 이 둘이 같은 스레드였고, 그래서 FM-1이 발생했다.

**실패 경로**

소비자가 500을 반환하면:

```
14:00:04.910  소비자 → 게이트웨이 : 500

14:00:04.912  원장 갱신
              attempt_count=1, status='PENDING', next_attempt_at=14:00:09.912

14:00:09.9xx  워커가 다시 집어서 재시도 (2회차)
              또 실패하면 attempt_count=2, next_attempt_at=+백오프

              ...

              attempt_count가 상한을 넘으면 status='DEAD'
              → 데드레터 알림 발송
              → 원본은 원장에 남아 있으므로 나중에 재생 가능
```

**중복 수신 경로**

제공자가 응답을 못 받아 같은 웹훅을 다시 보내면:

```
14:00:33.000  제공자 → 게이트웨이
              같은 요청, 같은 idempotency_key='evt_2b8f...'

14:00:33.006  INSERT 시도 → 유니크 인덱스 위반
              (slug, idempotency_key)에 UNIQUE

14:00:33.007  게이트웨이 → 제공자 : 200 OK
              ★ 저장하지 않는다. 소비자에게도 안 간다
              ★ 200을 주는 이유는 제공자의 재시도를 멈추기 위해서다
```

### 5.3 소비자 입장에서 무엇이 달라지나

| | 도입 전 | 도입 후 |
| --- | --- | --- |
| 엔드포인트 | 인터넷에 공개 | 내부망. 게이트웨이만 호출 |
| 서명 검증 | 소비자가 직접 | 게이트웨이가 대신 |
| 응답 시간 제약 | 제공자 타임아웃 (3초 등) | 게이트웨이 타임아웃 (설정 가능) |
| 실패 시 | 제공자 정책에 의존 | 게이트웨이가 재시도 |
| 해야 할 일 | 없음 | `X-Gateway-Event-Id`로 중복 차단 (15절) |

**소비자 코드는 거의 그대로 쓸 수 있다.** 서명 검증 부분을 지우고, 이벤트 ID 기반 중복 차단을 추가하면 된다.

---

## 6. 수신부

### 6.1 원본 바이트 보존

`@RequestBody PaymentEvent event`로 받으면 이미 문제가 발생한다.

요청 스트림은 Jackson이 소비했고, DTO를 재직렬화한 바이트는 원본과 다르다. 키 순서가 바뀌고, 공백이 사라지고, 숫자 표현이 달라진다. HMAC은 바이트 하나만 달라도 깨지므로 **재직렬화한 바디로는 서명 검증이 애초에 통과할 수 없다.**

해결:
- `byte[]` 또는 `String`으로 수신, 또는
- 필터 단에서 `ContentCachingRequestWrapper`로 감싸기

어느 쪽이든 서명 검증은 반드시 원본 바이트 위에서 수행한다.

원본을 보존해야 하는 두 번째 이유: 제공자가 페이로드 스키마를 변경하면 파싱해서 저장한 과거 데이터는 새 코드로 읽을 수 없다. 원본이 있으면 언제든 재파싱 가능하다.

### 6.2 서명 검증

기본형은 시크릿을 키로 한 HMAC-SHA256. 세 가지 논점이 있다.

**타임스탬프 윈도우.** 서명만 검증하면 재전송 공격이 가능하다. 유효한 요청을 가로챘다가 나중에 그대로 재전송해도 서명은 유효하다. 타임스탬프를 서명 대상에 포함시키고, 수신 시각과의 차이가 허용 범위(기본 5분) 이내일 때만 통과시킨다.

**상수 시간 비교.** 계산한 서명과 수신한 서명을 `equals()`로 비교하면 안 된다. 문자열 비교는 첫 불일치에서 조기 반환하므로 응답 시간 차이로 서명을 한 바이트씩 추론할 수 있다. `MessageDigest.isEqual`을 사용한다.

**시크릿 로테이션.** 시크릿을 교체하는 순간 구 시크릿으로 서명된 재시도 요청이 전부 실패한다. 구/신 시크릿을 동시에 허용하는 유예 기간이 필요하고, 이는 데이터 모델에 미리 반영해야 한다. 시크릿 컬럼이 하나면 로테이션 자체가 불가능하다.

### 6.3 제공자별 형식 차이

| 제공자 | 서명 헤더 | 이벤트 ID |
| --- | --- | --- |
| GitHub | `X-Hub-Signature-256` (서명만) | `X-GitHub-Delivery` 헤더 |
| Stripe | `Stripe-Signature` (타임스탬프 + 서명) | 페이로드 내 `event.id` |

서명 대상 문자열 조합 규칙도 다르다. 검증기를 전략 인터페이스로 분리하고 제공자별 구현을 주입한다.

```java
public interface SignatureVerifier {
    Provider provider();
    VerificationResult verify(byte[] rawBody, HttpHeaders headers, EndpointSecrets secrets);
}
```

### 6.4 응답 정책

- 서명 검증 실패 → 401. 재시도를 유발하지 않는다
- 알 수 없는 엔드포인트 → 404
- 검증 통과 후 저장 성공 → 200
- 멱등키 중복 → 200 (이미 받았음을 알리고 재시도를 멈추게 한다)
- 저장 실패 → 500. 제공자의 재시도를 유도한다

**목표 지연**: p99 50ms 이하.

---

## 7. 원장

### 7.1 데이터 모델

```sql
CREATE TABLE endpoint (
    id              BIGINT AUTO_INCREMENT PRIMARY KEY,
    slug            VARCHAR(64)  NOT NULL,
    provider        VARCHAR(32)  NOT NULL,
    secret_current  VARBINARY(255) NOT NULL,
    secret_previous VARBINARY(255) NULL,
    secret_rotated_at DATETIME(3) NULL,
    target_url      VARCHAR(1024) NOT NULL,
    retention_days  INT NOT NULL DEFAULT 7,
    enabled         BOOLEAN NOT NULL DEFAULT TRUE,
    created_at      DATETIME(3) NOT NULL,
    UNIQUE KEY uk_endpoint_slug (slug)
);

CREATE TABLE event (
    id              BIGINT AUTO_INCREMENT PRIMARY KEY,
    endpoint_id     BIGINT       NOT NULL,
    idempotency_key VARCHAR(255) NOT NULL,
    idempotency_source ENUM('PROVIDER_ID','BODY_HASH') NOT NULL,
    raw_body        MEDIUMBLOB   NOT NULL,
    raw_headers     JSON         NOT NULL,
    body_size       INT          NOT NULL,
    status          ENUM('PENDING','DELIVERING','DELIVERED','FAILED','DEAD') NOT NULL,
    attempt_count   INT          NOT NULL DEFAULT 0,
    next_attempt_at DATETIME(3)  NULL,
    received_at     DATETIME(3)  NOT NULL,
    replay_of       BIGINT       NULL,
    UNIQUE KEY uk_event_idem (endpoint_id, idempotency_key),
    KEY idx_event_dispatch (status, next_attempt_at),
    KEY idx_event_received (endpoint_id, received_at)
);

CREATE TABLE delivery_attempt (
    id              BIGINT AUTO_INCREMENT PRIMARY KEY,
    event_id        BIGINT       NOT NULL,
    attempt_no      INT          NOT NULL,
    started_at      DATETIME(3)  NOT NULL,
    duration_ms     INT          NULL,
    response_status INT          NULL,
    error_message   VARCHAR(512) NULL,
    KEY idx_attempt_event (event_id, attempt_no)
);
```

설계 메모:
- `raw_body`가 `MEDIUMBLOB`인 이유는 13절 참고
- `idx_event_dispatch`가 전달부 폴링 쿼리를 받는다. 상태와 시각의 복합 인덱스여야 스캔 범위가 좁아진다
- `replay_of`는 재생으로 생성된 이벤트가 원본을 가리킨다

### 7.2 상태 머신

```
      수신
        │
        ▼
    ┌────────┐
    │PENDING │◀──────────┐
    └───┬────┘           │ 백오프 후 재시도
        │                │
        ▼                │
  ┌───────────┐          │
  │DELIVERING │──실패────┘
  └─────┬─────┘
        │ 성공
        ▼
   ┌──────────┐      최대 시도 초과
   │DELIVERED │      ┌──────┐
   └──────────┘      │ DEAD │
                     └──────┘
```

### 7.2.1 좀비 회수

`DELIVERING`으로 바꾸고 소비자에게 POST를 보낸 뒤 응답을 받기 전에 게이트웨이 프로세스가 죽으면, 그 행은 `DELIVERING`인 채로 영구히 남는다. **폴링 쿼리는 `PENDING`만 보므로 아무도 다시 집지 않는다.**

```sql
-- 좀비 회수 배치. 1분 주기
UPDATE event
   SET status = 'PENDING', next_attempt_at = NOW(3)
 WHERE status = 'DELIVERING'
   AND updated_at < NOW(3) - INTERVAL 5 MINUTE;
```

회수하면 **이미 소비자가 처리한 것을 다시 보낼 수 있다.** 또 중복이고, 또 소비자 멱등성으로 푼다. 회수 임계 시간은 소비자 응답 타임아웃보다 충분히 커야 한다.

### 7.2.2 FAILED 상태를 두지 않는 이유

`FAILED`는 최종 실패가 아니라 "이번 시도 실패"를 기록하는 중간 상태로 쓰거나, 아예 두지 않고 `PENDING`으로 되돌려도 된다. **v1에서는 `FAILED`를 두지 않고 `DELIVERING` → `PENDING` 회귀로 단순화한다.** 재시도 횟수는 `attempt_count`로 판정한다.

### 7.3 멱등키 선택

**제공자가 이벤트 ID를 주는 경우.** 그대로 사용한다. 재시도 시 동일 값으로 오므로 정확하다.

**주지 않는 경우.** 바디 해시로 대체할 수 있으나 이는 근사치다. 같은 사용자가 같은 액션을 짧은 간격으로 두 번 하면 바디가 동일할 수 있고, 그러면 정상 이벤트를 중복으로 오판해서 버린다. 시간 윈도우를 붙여 완화할 수는 있으나 완전히 해결되지 않는다.

**이 한계는 감추지 않고 문서에 남긴다.** `idempotency_source` 컬럼으로 어느 방식이었는지 구분해 두면 사후 분석이 가능하다.

### 7.4 중복 차단 방식

조회 후 삽입(check-then-insert)은 동시 삽입 시 둘 다 통과한다. 유니크 인덱스에 의존해 삽입을 시도하고 `DuplicateKeyException`을 잡는다.

```java
try {
    eventRepository.insert(event);
} catch (DuplicateKeyException e) {
    return ResponseEntity.ok().build();  // 이미 받은 이벤트
}
```

### 7.5 멱등키 보관 기간

영구 보관하면 유니크 인덱스가 무한히 커진다. 짧게 지우면 중복이 샌다.

**멱등키의 수명은 제공자의 재시도 윈도우가 결정한다.** 제공자가 3일간 재시도하면 최소 3일은 보관해야 하고, 그보다 훨씬 길게 잡을 이유는 없다. 따라서 `endpoint.retention_days`로 제공자별 설정이 가능해야 한다.

정리 배치를 v1에 포함한다. 나중에 붙이면 이미 문제가 된 뒤다.

### 7.6 멱등성 계층 구분

게이트웨이의 멱등성과 소비자의 멱등성은 별개다.

```
제공자 ──(중복 가능)──▶ 게이트웨이 ──(중복 가능)──▶ 소비자
                        │                          │
                   제공자 이벤트 ID로         게이트웨이 이벤트 ID로
                      중복 차단                   중복 차단
```

게이트웨이가 제공자 쪽 중복을 걸러내도, 게이트웨이에서 소비자로 가는 전달 역시 at-least-once다. 게이트웨이는 자신의 이벤트 ID를 `X-Gateway-Event-Id` 헤더로 넘기고, 소비자가 그것으로 다시 걸러야 한다.

---

## 8. 전달부

### 8.1 전달 보장 수준

**exactly-once는 제공하지 않는다.** 분산 트랜잭션 없이는 불가능하다. at-least-once로 전달하고 소비자가 멱등하게 처리하는 것이 유일한 실용적 해법이다.

### 8.1.1 "실패"가 무엇을 뜻하나

게이트웨이에게 실패는 **소비자가 2xx를 주지 않았다**는 뜻뿐이다. 페이로드를 해석하지 않으므로 이유는 모른다.

실제로 발생하는 실패는 대부분 데이터 문제가 아니라 타이밍 문제다.

| 무엇이 잘못됐나 | 예 | 응답 | 재시도 효과 |
| --- | --- | --- | --- |
| 소비자가 지금 안 떠 있음 | 배포 중, 파드 재시작 | 커넥션 거부 | **있다** |
| 소비자가 느림 | DB 락 대기, 커넥션 풀 고갈 | 타임아웃 | **있다** |
| 소비자 쪽 일시 장애 | DB 커넥션 끊김, 외부 API 장애 | 5xx | **있다** |
| 소비자 코드 버그 | NPE | 5xx | 없다. 사람이 고쳐야 한다 |
| 페이로드가 처리 불가 | 존재하지 않는 주문 ID | 4xx | 없다 |

**위 세 줄이 실무에서 나오는 실패의 대부분이다.** 데이터는 멀쩡하고 받는 쪽 사정이 잠깐 나쁜 것뿐이라 재시도가 유효하다.

판정 규칙:

```
2xx                    → DELIVERED
4xx                    → 즉시 DEAD. 재시도해도 결과가 같다
5xx / 타임아웃 / 거부   → 재시도
```

**주의**: "아직 주문이 안 만들어진 것"과 "영원히 없는 것"은 다르다. 주문 생성보다 결제 웹훅이 먼저 도착하는 경쟁 상황이 실제로 있으며, 이때 4xx를 주면 영영 처리되지 않는다. **어느 쪽인지 판단하는 것은 소비자 몫이다.**

### 8.2 재시도와 백오프

지수 백오프에 지터를 더한다.

```
base_delay = min(initial * 2^attempt, max_delay)
actual_delay = jitter(base_delay)
```

**지터가 없으면 안 되는 이유**: 소비자가 5분간 다운됐다가 복구되면, 백오프만 있는 경우 밀린 이벤트의 재시도 시각이 동일 시점에 몰려서 소비자를 다시 죽인다(thundering herd). 지터를 넣으면 재시도 시점이 흩어진다.

지터 전략은 최소 두 가지를 구현하고 복구 곡선을 비교한다(11절 참고).

| 전략 | 계산 |
| --- | --- |
| full jitter | `random(0, base)` |
| decorrelated jitter | `min(max, random(initial, prev * 3))` |

### 8.3 서킷 브레이커

소비자가 지속적으로 5xx를 반환하는데 재시도를 계속 보내면 소비자 복구를 방해한다. 소비자(엔드포인트)별로 회로 상태를 관리하고, 열리면 전달을 중단하고 큐에만 적재한다.

### 8.4 순서 처리

가장 어려운 결정이며, 세 가지 선택지가 있다.

| 방식 | 순서 | 처리량 | 실패 시 |
| --- | --- | --- | --- |
| 파티션별 직렬 + 블로킹 | 보장 | 낮음 | 같은 파티션 전체가 막힘 (head-of-line blocking) |
| 파티션별 직렬 + 건너뛰기 | 깨짐 | 중간 | 실패 건만 데드레터, 나머지 진행 |
| 순서 미보장 + 소비자 판정 | 미보장 | 높음 | 영향 없음 |

**v1은 세 번째를 택한다.**

근거: 순서 보장은 처리량과 복잡도 비용이 큰 반면 얻는 것이 적다. 대부분의 상태 갱신은 last-writer-wins로 해결된다. 게이트웨이는 이벤트에 제공자 발생 시각을 실어 보내고, 소비자는 다음과 같이 처리한다.

```sql
UPDATE subscription
   SET plan = :plan, updated_at = :event_time
 WHERE id = :id
   AND updated_at < :event_time;
```

**이 결정은 문서에 명시하고 소비자 가이드에 반영한다.** "보장하지 않음"을 숨기면 소비자가 순서를 전제하고 짜다가 FM-4를 그대로 겪는다.

### 8.5 데드레터

최대 시도 횟수를 초과하면 `DEAD`로 전환한다. **데드레터 진입 자체가 알림 이벤트여야 한다.** 알림이 없으면 데드레터는 아무도 보지 않는 무덤이 된다.

---

## 9. 재생

### 9.1 목적

FM-6에 대한 대응이다. 소비 로직 버그를 수정한 뒤 과거 이벤트를 다시 흘린다.

### 9.2 전제

**재생은 소비자 멱등성을 전제로만 성립한다.** 재생하면 부작용도 다시 발생한다. 결제 이벤트를 재생하면 메일이 다시 나가고 재고가 또 빠진다. 게이트웨이 단독으로는 막을 수 없다. 이 경계를 명시적으로 선언한다.

### 9.3 기능

| 기능 | 내용 |
| --- | --- |
| 드라이런 | 실제 발송 없이 대상 건수와 목적지만 계산 |
| 범위 지정 | 시간 구간, 엔드포인트, 상태, 이벤트 타입 |
| 재생 표시 | `X-Gateway-Replay: true` 헤더 부착 |
| 재생 이력 | 누가 언제 어떤 범위를 재생했는지 기록 |

### 9.4 서명 재계산과 신뢰 경계 이동

원본 서명 헤더를 그대로 전달하면 소비자가 재검증할 수 있으나, 타임스탬프 윈도우 때문에 무조건 실패한다. 따라서 게이트웨이가 자신의 시크릿으로 재서명해서 보낸다.

이 결정에는 의미가 있다. **게이트웨이가 새로운 신뢰 경계가 된다.** 소비자는 이제 GitHub을 신뢰하는 것이 아니라 게이트웨이를 신뢰한다. 게이트웨이의 시크릿 관리 책임이 그만큼 커진다.

---

### 9.5 signature_mode — 도입 장벽과의 트레이드오프

서명을 어떻게 넘길지에 따라 소비자 수정량이 달라진다. **엔드포인트별로 고를 수 있게 한다.**

|  | passthrough | resign |
| --- | --- | --- |
| 게이트웨이가 하는 일 | 원본 서명 헤더를 그대로 전달 | 자기 시크릿으로 재서명 |
| 소비자 서명 검증 코드 | **그대로 둔다** | 게이트웨이 시크릿으로 교체 |
| 소비자가 신뢰하는 대상 | 원 제공자 | 게이트웨이 |
| 재생 | **불가.** 타임스탬프 윈도우 만료 | 가능 |
| 제공자 시크릿 위치 | 게이트웨이 + 소비자 양쪽 | 게이트웨이만 |

```yaml
endpoints:
  - slug: toss-payment
    signature_mode: passthrough   # passthrough | resign
```

**전환 전략**: 처음 붙일 때는 `passthrough`로 소비자 코드를 거의 건드리지 않고 도입하고, 안정화 후 `resign`으로 바꿔 재생을 켠다.

---

## 10. 기술 선택

### 10.1 큐: DB 테이블

MySQL 8.0의 `SELECT ... FOR UPDATE SKIP LOCKED`로 여러 워커가 경합 없이 작업을 가져간다.

```sql
SELECT id FROM event
 WHERE status = 'PENDING'
   AND next_attempt_at <= NOW(3)
 ORDER BY next_attempt_at
 LIMIT 50
 FOR UPDATE SKIP LOCKED;
```

**주의**: `SKIP LOCKED`는 MySQL 8.0부터다. 5.7에는 없다.

**선택 근거**

| 항목 | DB 큐 | Kafka / SQS |
| --- | --- | --- |
| 저장과 큐잉의 원자성 | 같은 트랜잭션. 아웃박스 불필요 | 두 시스템. 아웃박스 필요 |
| 조회/재생/상태 관리 | SQL로 해결 | 별도 저장소 필요 |
| 운영 요소 | 증가 없음 | 브로커 운영 추가 |
| 처리량 한계 | 낮음 | 높음 |
| 폴링 지연 | 있음 | 없음 |

이 프로젝트 규모에서 처리량과 폴링 지연은 제약이 되지 않는다. **DB 큐로 시작한다.**

**전환 기준**: 폴링 쿼리가 인덱스를 타고도 p99가 100ms를 넘거나, 초당 유입이 워커 확장으로 감당되지 않는 시점. 이 수치는 9절의 부하 테스트로 확정한다.

### 10.2 인덱스 주의

`SKIP LOCKED`를 걸어도 갭 락 때문에 예상보다 넓은 범위가 잠겨 워커가 서로 막힐 수 있다. `idx_event_dispatch (status, next_attempt_at)`가 실제로 잠금 범위를 좁히는지 실행 계획으로 확인해야 한다. **이 부분은 실측 전까지 불확실하다.**

---

## 11. 검증 계획

### 11.1 검증 환경 — 전부 직접 만든다

미리 존재해야 하는 것이 없다. 제공자와 소비자 모두 흉내 내야 하며, **오히려 그편이 실제 시스템보다 낫다.** 실제 제공자로는 소비자를 죽였다 살리는 상황을 만들 수 없다.

| 구성 | 정체 | 비용 |
| --- | --- | --- |
| 제공자 | 서명 붙여 POST 쏘는 셸 스크립트 | 반나절 |
| 게이트웨이 | 결과물 | 본체 |
| DB | `docker run mysql:8.0` | 5분 |
| 소비자 | 시나리오 제어 가능한 목 서버 | 하루 |
| 부하 도구 | k6 스크립트 | 2~3일 |

목 소비자는 아래를 시킬 수 있어야 한다. 마지막 항목이 없으면 "중복 0건"을 증명할 수 없다.

- 항상 2xx / 항상 5xx / 4xx
- N초 지연 후 응답
- 처음 M회 실패 후 성공
- 일정 시간 다운 후 부활
- **받은 이벤트 ID 기록**

**한계**: 이 구성은 폐쇄 루프다. 내가 만든 발신자가 내 게이트웨이로 쏘고 내 목 소비자가 받는다. 최소 하나는 실제 제공자를 붙이는 것을 권장한다. **GitHub 웹훅은 무료이고, 재시도하지 않는 제공자라 FM-2 검증에도 적합하다.** 로컬 개발 시에는 ngrok 등으로 터널링한다.


### 11.2 측정 항목

부하 도구(k6 또는 Gatling)로 측정하고 그래프를 남긴다.

| 항목 | 측정 방법 | 목표 / 관찰 |
| --- | --- | --- |
| 수신 응답 지연 | 초당 N건 유입 시 p50/p99 | p99 50ms 이하 |
| 복구 소진 시간 | 소비자 5분 다운 후 밀린 건 소진 시간 | 곡선 기록 |
| 지터 효과 | full / decorrelated / 없음 3종의 복구 시 RPS 곡선 | 그래프로 차이 확인 |
| 중복 차단 | 동일 이벤트 N건 동시 주입 후 처리 건수 | 정확히 1 |
| 페이로드 크기 영향 | 1KB / 100KB / 1MB 수신 지연 | 12절 임계값 결정 근거 |
| 큐 폴링 부하 | 워커 수별 폴링 쿼리 p99와 락 경합 | 8.1 전환 기준 산출 |

이 측정들은 실사용자 없이도 수행 가능하다. 합성 부하로 재현할 수 있는 것이 이 프로젝트의 장점이다.

---

## 12. v1 범위

### 12.1 포함

- 엔드포인트 등록 및 시크릿 관리(로테이션 포함)
- 제공자 2종 서명 검증 (GitHub + 1종)
- 원본 저장, 멱등키 기반 중복 차단
- 단일 목적지 전달
- 지수 백오프 + 지터 재시도, 데드레터
- 이벤트 조회, 단건 및 범위 재생, 드라이런
- 멱등키 정리 배치

### 12.2 제외 (의도적)

| 제외 항목 | 이유 |
| --- | --- |
| 순서 보장 | 비용 대비 효용이 낮다. 소비자 측 버전 판정으로 대체 권고 (7.4) |
| 팬아웃 1:N | v1은 1:1. 목적지별 상태 관리가 데이터 모델을 복잡하게 만든다 |
| 페이로드 변환/필터 규칙 | 범위가 무한히 확장된다. DSL을 만들기 시작하면 끝이 없다 |
| 멀티테넌시 | 인증과 격리가 프로젝트 규모를 두 배로 만든다 |
| 스트리밍/실시간 푸시 | 폴링으로 충분한 규모다 |

**"하지 않은 것과 그 이유"는 이 문서에서 가장 중요한 절이다.** 전부 만든 척하는 것보다 범위를 의도적으로 그었음을 보이는 편이 낫다.

---

## 13. 운영 시 함정

| 함정 | 내용 | 대응 |
| --- | --- | --- |
| 수신부가 DB에 묶임 | DB가 느려짐 → 수신 응답 지연 → 제공자 타임아웃 → 재시도 증가 → DB 부하 증가. 악순환이 닫힌다 | 수신부는 최소 쓰기만 수행. 필요 시 앞단 버퍼 검토 |
| 큰 페이로드 | 대부분 수 KB지만 GitHub push는 커밋이 많으면 MB 단위 | 임계값 이상은 오브젝트 스토리지로 분리. 임계값은 9절에서 결정 |
| 시계 오차 | 타임스탬프 윈도우 검증이 서버 시계에 의존. NTP가 밀리면 정상 요청이 전부 거부됨 | 시계 드리프트 모니터링, 윈도우를 지나치게 좁히지 않음 |
| 데드레터 무덤 | 알림 없으면 아무도 안 봄 | 데드레터 진입을 알림 이벤트로 취급 |
| 멱등키 인덱스 팽창 | 정리 배치가 없으면 몇 달 뒤 문제화 | v1에 정리 배치 포함 (6.5) |

---

## 14. 알려진 한계

1. **exactly-once를 제공하지 않는다.** at-least-once + 소비자 멱등성이 전제다.
2. **순서를 보장하지 않는다.** 소비자가 이벤트 시각으로 판정해야 한다.
3. **제공자 이벤트 ID가 없는 경우 멱등성이 근사치다.** 바디 해시 방식은 동일 페이로드의 정상 중복을 오판할 수 있다.
4. **재생은 소비자 멱등성 없이는 안전하지 않다.** 게이트웨이 단독으로 부작용을 막을 수 없다.
5. **재생 시 신뢰 주체가 게이트웨이로 이동한다.** 소비자는 원 제공자의 서명을 검증할 수 없다.
6. **DB 큐의 처리량 상한은 미측정이다.** 11절 완료 후 갱신 예정.
7. **게이트웨이가 새로운 단일 장애점이 된다.** 도입 전에는 제공자와 소비자가 직결이라 중간이 없었다. 짧은 다운은 제공자 재시도가 흡수하지만, **재시도하지 않는 제공자(GitHub)에게는 그 시간 동안 온 것이 유실된다.** FM-2를 풀려고 만든 물건이 같은 문제를 자기 자신에게 갖는다. 게이트웨이의 가용성 요구사항이 소비자보다 높다.
8. **별도 서버가 유일한 답은 아니다.** 3절 참고. 조건이 맞지 않으면 애플리케이션 내부 아웃박스나 라이브러리 형태가 낫다.

---

## 15. 소비자 가이드 (요약)

게이트웨이를 사용하는 쪽이 반드시 지켜야 할 것.

1. `X-Gateway-Event-Id` 헤더로 중복을 차단한다. 게이트웨이의 중복 차단만 믿으면 안 된다.
2. 이벤트 발생 시각으로 순서를 판정한다. 도착 순서를 신뢰하지 않는다.
3. 처리 성공 시 2xx, 재시도가 필요하면 5xx, 재시도가 무의미하면 4xx를 반환한다.
4. `X-Gateway-Replay: true`인 요청에서 알림성 부작용(메일, 푸시)을 건너뛸지 결정한다.

---

## 부록 A. 용어

기본 용어(제공자, 소비자, 수신부, 원장, 전달부, 원본 바이트, 서명 검증)는 2.4절에 있다. 아래는 그 외.

| 용어 | 정의 |
| --- | --- |
| 멱등키 | 같은 이벤트의 재수신을 식별하는 키 |
| 데드레터 | 최대 재시도를 소진하고 전달을 포기한 이벤트 |
| 지터 | 재시도 시각에 무작위성을 더해 동시 재시도를 분산시키는 기법 |
| 파티션 키 | 순서를 보장할 단위를 정하는 키 (예: 구독 ID) |
| at-least-once | 최소 한 번 전달을 보장하되 중복은 허용하는 전달 방식 |
| HMAC | 비밀키를 함께 넣어 계산하는 해시. 키를 모르면 같은 값을 만들 수 없다 |
