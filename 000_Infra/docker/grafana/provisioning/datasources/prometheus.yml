apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    # uid 를 고정해야 파일로 프로비저닝하는 대시보드가 이 데이터소스를 안정적으로 가리킨다
    uid: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
